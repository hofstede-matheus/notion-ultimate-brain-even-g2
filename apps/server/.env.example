# Server
# Notion credentials are per-user, entered in the app's Settings screen (see
# apps/glasses/src/tenant-config.ts) and sent per-request via the
# X-Notion-Config header. The server only needs the local dev port.
PORT=3210
